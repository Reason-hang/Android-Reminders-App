# 当前交接

> 状态：当前事实入口
> 更新时间：2026-07-22
> 代码提交：`b61395b docs: add current project handoff`
> 代码修复基线：`445876e fix: harden v1.3 reminder alerts`

## 目录

- [当前版本](#当前版本)
- [已验证事实](#已验证事实)
- [未完成项](#未完成项)
- [产品与架构不变量](#产品与架构不变量)
- [执行顺序](#执行顺序)
- [交接前更新规则](#交接前更新规则)

## 当前版本

| 项目 | 当前事实 |
|---|---|
| App | Android Reminders App |
| 包名 | `com.reminder.local` |
| 版本 | v1.3，versionCode 4 |
| 数据库 | Room v4 |
| 分支 | `main` |
| 工作区 | 2026-07-22 接手时干净 |
| 目标设备 | 红米 K80 Pro / HyperOS / Android 16 |

## 已验证事实

证据记录于 2026-07-22：

- 20 个测试类，63/63 JVM 测试通过。
- `lintDebug` 通过，保留 55 warnings、3 hints。
- `assembleRelease` 通过。
- APK 已复核包名、版本、权限、SHA-256 和 v2 签名。
- Release 使用 Debug 签名，只适合个人直装和测试。
- 未声明 `INTERNET`；当前没有账号、网络、云同步或远程数据链路。

## 未完成项

| 优先级 | 项目 | 完成标准 |
|---|---|---|
| P0 | 红米 K80 Pro 真机强提醒 | A1-A14、C1-C5、系统恢复场景均有设备日志和截图/录屏 |
| P1 | v3→v4 Room 迁移 | 使用 `MigrationTestHelper` 验证字段删除、数据保留和 `alarmId` 修复 |
| P1 | 取消闹钟失败补偿 | 完成/删除后的取消失败可持久化、可重试、可观测 |
| P2 | 正式签名 | 用户保管正式 keystore，并验证覆盖升级策略 |
| P2 | Lint 清理 | 按风险处理 55 warnings、3 hints，不把非阻断警告伪装成零问题 |

## 产品与架构不变量

- `ADVANCE`、`DUE`、`SNOOZE` 独立；只有 `DUE` 推进重复周期。
- 每个操作携带 `reminderId + alarmId + kind + occurrenceTime`。
- 旧 occurrence 只能结束自身打断，不能停止或修改新 occurrence。
- `triggerTime` 是重复模板，`nextTriggerTime` 是实际下一次时间。
- Room 与 AlarmManager 采用补偿式一致性，不假设跨系统 ACID 事务。
- 强提醒验收链为：Alarm 注册 → Receiver → 前台服务 → 声音/震动 → WakeLock → 全屏页 → 锁屏业务通知。
- 不增加 `INTERNET`，不提交 APK、keystore、密码、token 或 `local.properties`。

## 执行顺序

1. 先确认 `git status -sb` 和 `git log -1 --oneline`。
2. 目标设备连接后，按 [真机验收清单](../03-测试与验收/真机验收清单.md) 执行 P0。
3. 真机失败时先保存 logcat、设备状态和复现步骤，再修改代码。
4. 代码或配置变化后，重新执行相关测试、Lint、构建、APK 元数据检查和 `git diff --check`。
5. 只有真机证据完整后，才能写“红米 K80 Pro / HyperOS 完整产品验收通过”。

## 交接前更新规则

- 当前事实必须绑定版本、提交、验证日期和实际命令。
- 历史结果只能放在 Git 历史，不得复制到当前结论。
- 修改 Alarm、通知、重复、Room 或 APK 交付逻辑时，同步更新相关测试和验收文档。
- 文档目录或文件名变化后，必须同步更新 [文档总索引](../00-文档总索引.md) 与 README。
