# 当前项目状态与交接

> 状态：v1.7 当前事实入口
> 更新时间：2026-08-10
> Git 提交：交接时以 `git log -1 --oneline` 实际输出为准

## 目录

- [当前基线](#当前基线)
- [本次完成](#本次完成)
- [验证证据](#验证证据)
- [未完成项](#未完成项)
- [架构不变量](#架构不变量)
- [接手顺序](#接手顺序)

## 当前基线

| 项目 | 当前事实 |
|---|---|
| App | Android Reminders App |
| 包名 | `com.reminder.local` |
| 版本 | v1.7、versionCode 8 |
| 数据库 | Room v4，无本次 Schema 变化 |
| 分支 | `main` |
| 目标设备 | 红米 K80 Pro、HyperOS、Android 16 |
| 网络 | 未声明 `INTERNET`，纯本地运行 |

## 本次完成

- 修复强提醒无自动结束机制：每个实例启动十分钟计时，超时停止铃声、振动和前台状态。
- 超时关闭匹配全屏页、撤销 App 保亮并保留静音通知；Activity 增加页面级兜底。
- 旧 ADVANCE 计时器不能停止新的 DUE 或其他提醒实例。
- 版本升级为 v1.7，生成并复核 `ReminderApp-v1.7.apk`。
- 按跨平台 Markdown 规范重建文档目录，新增 PRD 和 AI 自主决策记录，重写 README、总索引、交接及验证文档。

## 验证证据

2026-08-10 当前源码：

- 24 个 JVM 测试类，83/83 通过，失败、错误、跳过均为 0。
- `compileDebugAndroidTestKotlin` 通过；迁移仪器测试未在设备或模拟器执行。
- `lintDebug` 通过：0 error、59 warnings、3 hints。
- `assembleRelease` 通过。
- APK 为 `com.reminder.local`、`1.7 (8)`、48,434,617 bytes、v2 Android Debug 签名、未声明 `INTERNET`。
- 目标手机未连接，未执行本次真机验证。

## 未完成项

| 优先级 | 项目 | 完成标准 |
|---|---|---|
| P0 | 红米 K80 Pro 强提醒回归 | 锁屏、熄屏、解锁、后台、ADVANCE→DUE 和 HyperOS 权限场景有日志及录屏 |
| P0 | 十分钟自动结束真机验收 | 声音、振动、Service 和全屏页全部结束，旧计时器不误关新实例，屏幕不再被 App 保亮 |
| P1 | Room v3→v4 仪器测试 | 在设备或模拟器实际执行 `MigrationTestHelper` |
| P1 | 取消闹钟失败补偿 | 完成或删除后的取消失败可持久化、可重试、可观测 |
| P2 | 正式签名 | 用户保管正式 keystore，并验证覆盖升级策略 |
| P2 | Lint 维护 | 按风险处理 59 warnings、3 hints，不与功能修复混改 |

## 架构不变量

- `ADVANCE`、`DUE`、`SNOOZE` 独立，只有 `DUE` 推进重复周期。
- 事件、操作和超时均按 `reminderId + alarmId + kind + occurrenceTime` 隔离。
- Service 管理强提醒媒体与会话；Activity 只负责展示和用户交互。
- `triggerTime` 是重复模板，`nextTriggerTime` 是实际下一次时间。
- Room 与 AlarmManager 使用补偿式一致性，不假设跨系统事务。
- 不增加网络能力，不提交 APK、签名文件、密钥或本机配置。

## 接手顺序

1. 阅读 [README](../../README.md)、[产品需求文档](../01-产品文档/01-产品需求文档.md) 和 [AI 自主决策记录](./01-AI自主决策记录.md)。
2. 执行 `git status -sb` 与 `git log -1 --oneline`，以当前 Git 状态覆盖本文可能过期的信息。
3. 连接目标手机后，优先执行 [真机验收清单](../05-测试与验收/02-真机验收清单.md)。
4. 真机失败时先保存 logcat、设备锁屏状态、权限状态和复现时间，再修改代码。
5. 代码变化后重新执行 JVM、仪器源码编译、Lint、Release 构建、APK 元数据和文档链接检查。
