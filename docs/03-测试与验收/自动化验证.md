# 自动化验证

> 状态：当前验证记录
> 更新时间：2026-07-23
> 版本：v1.4（versionCode 5）

## 目录

- [当前结论](#当前结论)
- [已执行验证](#已执行验证)
- [覆盖范围](#覆盖范围)
- [尚缺验证](#尚缺验证)

## 当前结论

2026-07-23 记录的当前 v1.4 自动化结果为：22 个测试类、69/69 JVM 测试通过，失败/错误/跳过均为 0；`lintDebug` 和 `assembleRelease` 通过。Lint 有 0 error、59 warnings、3 hints。

## 已执行验证

```bash
./gradlew clean testDebugUnitTest lintDebug assembleRelease --console=plain --warning-mode=none

./gradlew :app:compileDebugAndroidTestKotlin --console=plain --warning-mode=none
```

同时记录了 `git diff --check`、Manifest/XML、Room Schema JSON、敏感信息和禁止项扫描结果。上述证据证明代码层和构建层状态，不证明目标真机强提醒体验。

## 覆盖范围

- Alarm：ADVANCE、DUE、SNOOZE 事件隔离和 requestCode 隔离。
- 重复：occurrence 条件推进、月末裁剪、提前量校验和结束日期。
- 一致性：新增、编辑、完成、删除、恢复过程的失败补偿。
- 并发：旧提醒操作不能停止新提醒；旧一次性通知不能完成已改期提醒。
- 通知：服务通知、业务通知、声音/震动失败降级和交互策略。
- UI：时间滚轮、编辑保存入口和通知入口状态契约。
- 审计修复：设备时区工作日、领域内容长度校验、精确闹钟权限返回刷新和 v3 Schema 留存。
- Room：v3→v4 仪器测试覆盖 priority 删除和重复 `alarmId` 修复；源码已编译。

## 尚缺验证

- Android 仪器层的 Notification、Manifest、Receiver 测试。
- 在设备或模拟器上实际执行 v3→v4 `MigrationTestHelper` 测试。
- 红米 K80 Pro 的锁屏、后台、清任务、重启、权限变化和并发验收。
- APK 覆盖安装后的旧数据完整性。
