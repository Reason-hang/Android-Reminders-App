# 审计问题处理记录

> 状态：当前处理记录
> 更新时间：2026-07-23
> 版本：v1.4（versionCode 5）
> 输入：外部代码审计报告《Android 离线强提醒 App —— 代码审计报告》

## 目录

- [核实结论](#核实结论)
- [已修复问题](#已修复问题)
- [未作为故障处理的建议](#未作为故障处理的建议)
- [验证结果与边界](#验证结果与边界)

## 核实结论

报告列出的 N1-N4 在当前代码中均可复现或直接核实，根因判断正确。报告摘要提及两项 S4，但正文未提供编号、文件位置或复现条件，因此不能作为可验证故障进入修复范围。

## 已修复问题

| 编号 | 根因 | 修复 | 回归证据 |
|---|---|---|---|
| N1 | `WORKDAYS` 固定 `Asia/Shanghai`，其他规则使用设备时区 | 每次计算统一读取设备时区，并传给工作日和月度规则 | 非北京时间设备时区下，周五工作日正确推进到当地周一 |
| N2 | 标题/备注上限只在编辑 UI | 新增 `ReminderContentValidator`，新增和编辑 UseCase 均先校验 | 超长标题、超长备注被拒绝，且不写库、不替换闹钟 |
| N3 | 精确闹钟权限只随提醒列表 Flow 重算 | ViewModel 使用独立 StateFlow；列表首次进入和 `ON_RESUME` 时刷新 | 源码契约测试确认生命周期刷新入口 |
| N4 | 缺少可供 v3→v4 迁移测试使用的历史 Schema | 从仓库 v3 历史源码实际生成 `3.json`；新增 Room `MigrationTestHelper` 仪器测试 | Schema 契约测试通过；仪器测试源码已编译 |

## 未作为故障处理的建议

- `BootReceiver` 的恢复策略可在未来评估 WorkManager，但当前 `goAsync()`、异常隔离和 `finish()` 逻辑没有已复现缺陷。
- 长期停机后的重复计算可在未来做性能优化；现有提醒频率和个人使用规模下不是当前性能问题。
- 枚举 Converter 可增加损坏数据兜底；当前 App 内没有写入非法枚举值的入口。

## 验证结果与边界

- 先新增 6 个针对 N1-N4 的失败测试，定向执行时 22 个测试中 6 个失败；修复后同批测试通过。
- v1.4 全量 JVM 测试为 22 个类、69/69 通过，失败/错误/跳过均为 0。
- `lintDebug` 无 error，保留 59 warnings、3 hints。
- `assembleRelease` 通过，APK 已复核包名 `com.reminder.local`、版本 `1.4 (5)`、v2 签名和 SHA-256。
- 新增的 v3→v4 Room 仪器测试已编译，但未在真机或模拟器执行；红米 K80 Pro 强提醒链路仍需要按 [真机验收清单](真机验收清单.md) 采集 A 级证据。
