# 代码审计与风险

> 状态：v1.14 当前风险摘要；旧版强提醒证据不替代本版回归
> 更新时间：2026-08-16

## 目录

- [审计结论](#审计结论)
- [已修复问题](#已修复问题)
- [平台行为边界](#平台行为边界)
- [残余风险](#残余风险)
- [证据边界](#证据边界)

## 审计结论

当前未发现阻断 Release 构建的代码错误。v1.6 以前确认的事件身份、重复推进、迁移和 Converter 问题保持修复；v1.7 新增实例级十分钟自动结束，解决铃声、振动和全屏页可无限持续的问题。目标设备行为仍需真机采证。

v1.14 在 v1.13 基础上新增编辑页撤销/重做，Room v5 迁移、删除/恢复/永久删除、排序和编辑历史的 JVM 覆盖已通过。当前仍不能把源码与构建结果替代 v1.14 目标设备触摸、迁移和强提醒验收。

v1.9 使用红米系统日志确认并移除了 Android 16 BAL 拦截路径。v1.11 修正未来计划误报并补齐 SNOOZE 调度证据。v1.12 在用户明确授权下新增解锁应用悬浮强提醒；专用真机测试和真实 ADVANCE、DUE、SNOOZE 均已确认整页显示。

## 已修复问题

| 范围 | 根因 | 当前修复 |
|---|---|---|
| 强提醒超时 | 铃声和振动无限循环，Activity 持有 `FLAG_KEEP_SCREEN_ON` 且无定时退出 | Service 管理十分钟实例计时；超时停止媒体与前台状态，通知 Activity 清除保亮并关闭 |
| 超时并发 | 旧 ADVANCE 或旧提醒的计时器可能误关新实例 | 超时按 `alarmId + kind + occurrenceTime` 完全匹配；新实例取消旧计时 |
| DUE 语义 | 提前提醒未关闭时，正式到点需要再次强提醒 | 同一 `alarmId` 的新 occurrence 重启播放和投递链路 |
| 后台全屏 | Service 自行 `PendingIntent.send()` 在 Android 16 被 BAL 拦截，页面不出现 | 删除 Service 与备用通知的自拉起；系统通知成为唯一 FullScreenIntent 投递者 |
| 解锁横幅 | Android 在设备使用中把 FullScreenIntent 降级为 Heads-up，App 无 Activity 生命周期 | 锁屏保留 FullScreenIntent；解锁且显式授权后由前台服务显示 `TYPE_APPLICATION_OVERLAY` 强提醒页 |
| 稍后未全屏 | SNOOZE 实际准时到 Receiver 且媒体成功，但触发时设备已解锁 | SNOOZE 复用 v1.12 解锁悬浮视觉路由，不改调度语义 |
| 诊断语义 | 静音配置被写为 `playback_started=started`；Activity 生命周期不区分来源 | 记录请求/实际启动与 `launchSource`；旧记录只标为证据不足 |
| 并发诊断 | 单一强提醒服务被后续提醒接管或停止时，旧时间线无法说明原因 | 记录 `alert_preempted` 和具体停止来源，解析器不再归因到自动超时 |
| 通知路由 | 保留通知点击直接进入编辑页 | 新旧入口均只进入提醒列表 |
| Room 迁移 | 历史脏 `alarmId` 可能使唯一索引失败 | v3→v4 按确定顺序分配连续负值，保留 Schema 和迁移测试 |
| 枚举读取 | 未知持久化文本会使查询 Flow 异常 | Converter 使用保守默认值；枚举改名必须配套 Migration |
| 时区和校验 | 工作日固定时区、长度限制仅在 UI | 统一设备时区，标题和备注限制下沉 UseCase |

## 平台行为边界

- 锁屏 FullScreenIntent 的最终结果仍受全屏通知权限、通知渠道、SystemUI 和 HyperOS 策略控制。
- 解锁悬浮层位于普通 Activity 之上、关键系统窗口之下；必须有特殊权限，用户可随时撤销。窗口成功添加由 App、WindowManager 和截图三层证据确认。
- 屏幕关闭不等于 Keyguard 已锁定；真机测试必须分别记录两种状态。
- 自动结束撤销 App 保亮，不使用设备管理或系统签名权限强制关屏。

## 残余风险

| 优先级 | 风险 | 后续动作 |
|---|---|---|
| P0 | v1.14 锁屏 DUE 页面和十分钟自动结束未完成真机复验 | 按 A1-A17、T1-T5 采集新版 ZIP、系统日志和录屏 |
| P0 | HyperOS 全屏、锁屏、后台行为存在设备策略差异 | 按 A1-A15 分状态验收 |
| P1 | v3→v4、v4→v5 迁移测试只编译未设备运行 | 在设备或模拟器执行 |
| P1 | `AlarmManager.cancel()` 极端失败无持久化重试 | 增加可观测补偿队列 |
| P2 | Release 使用 Debug 签名 | 正式发布前切换受控 keystore |
| P2 | 67 warnings、4 hints | 当前无 error；与功能修复分批处理 |
| P1 | v4→v5 迁移与回收站/排序/编辑历史尚未在 v1.14 真机执行 | 先执行 `MigrationTestHelper`，再按 D1–D8 验收 |
| P1 | 精细拖拽当前不做自动滚屏 | 十几至二十条事项的大跨度调整使用多选置顶/置底；真机确认可见区精细调整体验 |

## 证据边界

v1.14 的 JVM、仪器源码编译、Lint 与 Release 证明当前源码和构建状态；当前未连接 ADB，未发生本版覆盖安装。v1.12/v1.13 的解锁仪器用例、WindowManager、截图和真实 ADVANCE、DUE、SNOOZE 仅证明旧版路径曾可用，仍不证明 v1.14 锁屏 DUE、完整十分钟结束、回收站、编辑页撤销/重做或所有 HyperOS 权限组合。
