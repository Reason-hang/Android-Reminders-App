# 自动化验证

> 状态：v1.15 当前自动化记录
> 验证日期：2026-08-16
> 编译策略：JDK 17、`-Dkotlin.compiler.execution.strategy=in-process -Dkotlin.incremental=false`

## 结果

| 验证项 | 实际结果 |
|---|---|
| JVM | 112 个通过，失败 0、错误 0 |
| Android 仪器测试源码 | `:app:compileDebugAndroidTestKotlin` 通过 |
| Room 迁移 | v6 schema 已生成；v5→v6 空状态清洗迁移和真实 `getAllPending()` 映射仪器用例已编译，未因当前无 ADB 设备而实际执行 |
| Lint | `lintDebug` 通过，0 error、67 warnings、4 hints |
| Release | `assembleRelease` 生成 `app-release.apk`；`aapt` 读回 `1.15 (16)` |
| APK 验签 | v2 签名有效；签名者为 Android Debug（仅个人直装/测试） |
| 目标真机 | 未执行：当前 `adb devices -l` 返回无设备，不能继承 v1.12 结果 |

## 实际命令

```bash
JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home \
./gradlew testDebugUnitTest :app:compileDebugAndroidTestKotlin \
  --rerun-tasks --no-daemon --console=plain \
  -Dkotlin.compiler.execution.strategy=in-process -Dkotlin.incremental=false

./gradlew lintDebug --no-daemon --console=plain \
  -Dkotlin.compiler.execution.strategy=in-process -Dkotlin.incremental=false

./gradlew assembleRelease --no-daemon --console=plain \
  -Dkotlin.compiler.execution.strategy=in-process -Dkotlin.incremental=false
```

本机 Kotlin 增量缓存曾出现文件锁冲突，已停止残留 daemon 后使用进程内、非增量编译取得通过结果；这是构建环境问题，不是源码规则错误。Lint 与 Release 按串行单任务执行，避免并发读取 KAPT 临时产物。

## 覆盖范围

- 500 字标题边界、编辑页使用同一领域校验；编辑页撤销/重做、历史分支和连续文字合并。
- 回收站删除、未来待办恢复后的重新调度、永久删除保护、批量排序唯一性。
- Room v4→v5 回收站/排序迁移、v5→v6 空状态清洗迁移、v6 schema、正常列表和待调度查询排除 `DELETED`。
- `Converters.stringToStatus(null)`、`statusToString(null)` 与 Room 实际 `getAllPending()` 对空 `statusBeforeDelete` 的映射契约。
- 既有 ADVANCE/DUE/SNOOZE、十分钟自动结束、重复计算、通知路由与诊断契约。

## 未覆盖范围

- `MigrationTestHelper` 的实际设备/模拟器运行。
- 红米 v1.14→v1.15 覆盖升级后正常提醒空 `statusBeforeDelete` 的冷启动、列表和闹钟重建验收。
- 锁屏、熄屏、后台、权限撤销、ADVANCE→DUE、SNOOZE 和十分钟自动结束的 v1.15 真机回归。

因此当前结论是“源码、JVM、仪器源码编译、Lint 与 Release 通过”，不是“v1.15 红米完整验收通过”。
