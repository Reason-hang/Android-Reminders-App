# 自动化验证

> 状态：v1.7 当前验证记录
> 验证日期：2026-08-10

## 目录

- [验证结论](#验证结论)
- [实际命令](#实际命令)
- [覆盖范围](#覆盖范围)
- [未覆盖范围](#未覆盖范围)

## 验证结论

| 验证项 | 当前结果 |
|---|---|
| JVM 测试 | 24 个测试类，83/83 通过，失败 0、错误 0、跳过 0 |
| Android 仪器测试源码 | `compileDebugAndroidTestKotlin` 通过 |
| Lint | `lintDebug` 通过，0 error、59 warnings、3 hints |
| Release | `assembleRelease` 通过 |
| 真机/模拟器 | 未连接、未执行 |

## 实际命令

```bash
JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home \
ANDROID_HOME=/opt/homebrew/share/android-commandlinetools \
./gradlew testDebugUnitTest :app:compileDebugAndroidTestKotlin \
  --rerun-tasks --console=plain --warning-mode=none
```

```bash
JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home \
ANDROID_HOME=/opt/homebrew/share/android-commandlinetools \
./gradlew lintDebug assembleRelease \
  --rerun-tasks --console=plain --warning-mode=none
```

两条命令均以退出码 0 完成。测试数量来自 `app/build/test-results/testDebugUnitTest/*.xml` 汇总，不是沿用旧报告。

## 覆盖范围

- Alarm：ADVANCE、DUE、SNOOZE、requestCode 和 PendingIntent 身份隔离。
- 强提醒：前后台启动策略、声音/振动降级、通知交互和实例并发策略。
- v1.7 超时：十分钟常量、旧超时不能结束新实例、Service 自动结束链路和 Activity 清除保亮契约。
- 重复：设备时区、月末裁剪、结束日期、occurrence 条件推进和提前量校验。
- 一致性：新增、编辑、完成、删除和批量恢复的失败补偿。
- Room：v3 Schema、v3→v4 迁移源码、`alarmId` 临界处理和枚举损坏数据降级。
- UI 契约：时间滚轮、权限返回刷新和全屏页新 Intent 处理。

## 未覆盖范围

- Android 仪器测试尚未在设备或模拟器运行。
- 红米 K80 Pro 的锁屏、熄屏、解锁、后台、清任务、重启和权限变化。
- 十分钟真实时间经过后的声音、振动、Service、Activity 和屏幕状态。
- v1.6 覆盖安装 v1.7 后的旧数据与历史提醒完整性。

因此当前结论是“代码和构建层通过”，不是“目标设备产品验收通过”。
